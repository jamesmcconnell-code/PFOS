'use client';import {useEffect,useState} from 'react';import {api,money} from '@/lib/api';import {Protected} from '@/components/protected';
export default function Dashboard(){const [d,setD]=useState<any>();useEffect(()=>{api('/dashboard').then(setD).catch(()=>{})},[]);const cards=[['Net worth','net_worth'],['Cash available','cash_available'],['Debt total','debt_total'],['Monthly savings','monthly_savings']];return <Protected><header><p className="label">Overview</p><h1 className="text-3xl font-bold">Financial command center</h1></header>{!d?<p className="mt-8">Loading…</p>:<><section className="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([l,k])=><div className="card" key={k}><p className="label">{l}</p><p className="metric mt-2">{money(d[k])}</p></div>)}</section><section className="mt-6 grid gap-5 lg:grid-cols-3"><div className="card"><h2 className="font-semibold">This month</h2><dl className="mt-4 space-y-3 text-sm"><div className="flex justify-between"><dt>Income</dt><dd>{money(d.monthly_income)}</dd></div><div className="flex justify-between"><dt>Expenses</dt><dd>{money(d.monthly_expenses)}</dd></div><div className="flex justify-between font-semibold"><dt>Savings rate</dt><dd>{d.savings_rate}%</dd></div></dl></div><div className="card"><h2 className="font-semibold">Crypto investments</h2>{d.crypto_assets?.length?d.crypto_assets.map((x:any)=><div className="mt-3 flex justify-between text-sm" key={x.symbol}><span>{x.symbol}</span><b>{Number(x.amount).toLocaleString(undefined,{maximumFractionDigits:8})}</b></div>):<p className="mt-3 text-sm text-slate-500">No crypto assets linked.</p>}</div><div className="card"><h2 className="font-semibold">Goals</h2>{d.goals.map((g:any)=><div className="mt-4" key={g.id}><div className="flex justify-between text-sm"><span>{g.name}</span><span>{g.progress}%</span></div><div className="mt-2 h-2 overflow-hidden rounded bg-slate-100"><div className="h-full bg-mint" style={{width:`${Math.min(g.progress,100)}%`}}/></div></div>)}</div></section></>}</Protected>}
