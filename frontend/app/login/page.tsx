'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {api} from '@/lib/api';
export default function Login(){const [email,setEmail]=useState('admin@example.com'),[password,setPassword]=useState('change-me-now'),[error,setError]=useState(''),[register,setRegister]=useState(false),router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();try{const r=await api('/auth/'+(register?'register':'login'),{method:'POST',body:JSON.stringify(register?{email,password,display_name:email.split('@')[0]}:{email,password})});localStorage.setItem('pfos_token',r.access_token);router.push('/dashboard')}catch(e){setError(e instanceof Error?e.message:'Unable to sign in')}}return <main className="grid min-h-screen place-items-center bg-navy p-5"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl bg-white p-7 shadow-xl"><h1 className="text-3xl font-bold">PF<span className="text-emerald-500">OS</span></h1><p className="mb-6 mt-2 text-sm text-slate-500">Your private household money system.</p>{error&&<p className="mb-3 rounded bg-red-50 p-2 text-sm text-red-700">{error}</p>}<label className="label">Email</label><input className="mb-4 mt-1 w-full rounded-lg border p-2" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/><label className="label">Password</label><input className="mb-5 mt-1 w-full rounded-lg border p-2" value={password} onChange={e=>setPassword(e.target.value)} type="password" required/><button className="w-full rounded-lg bg-navy py-2.5 font-medium text-white">{register?'Create private household':'Sign in'}</button><button type="button" onClick={()=>setRegister(!register)} className="mt-4 w-full text-sm text-slate-600">{register?'Already have an account? Sign in':'New here? Create an account'}</button></form></main>}
