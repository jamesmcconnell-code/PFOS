'use client';import {useEffect,useState} from 'react';import {api} from '@/lib/api';import {Protected} from '@/components/protected';export default function Settings(){const [me,setMe]=useState<any>(),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState('');useEffect(()=>{api('/auth/me').then(x=>{setMe(x);setName(x.display_name);setEmail(x.email)})},[]);async function save(e:React.FormEvent){e.preventDefault();try{const x=await api('/auth/me',{method:'PATCH',body:JSON.stringify({display_name:name,email,password:password||undefined})});setMe(x);setPassword('');setMessage('Profile saved.')}catch(e){setMessage(e instanceof Error?e.message:'Could not save profile')}}return <Protected><h1 className="text-3xl font-bold">Settings</h1><form onSubmit={save} className="card mt-6 max-w-xl"><h2 className="font-semibold">Your profile</h2>{message&&<p className="mt-3 text-sm text-slate-600">{message}</p>}<label className="label mt-4 block">Display name</label><input className="mt-1 w-full rounded border p-2" value={name} onChange={e=>setName(e.target.value)} required/><label className="label mt-4 block">Email</label><input className="mt-1 w-full rounded border p-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label className="label mt-4 block">New password</label><input className="mt-1 w-full rounded border p-2" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Leave blank to keep current password"/><button className="mt-5 rounded bg-navy px-4 py-2 text-white">Save profile</button></form></Protected>}
