const base=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000/api/v1';
export async function api(path:string, init:RequestInit={}){const token=typeof window!=='undefined'?localStorage.getItem('pfos_token'):null;const view=typeof window!=='undefined'?localStorage.getItem('pfos_view_user_id'):null;if(view&&['/dashboard','/accounts','/transactions','/forecast','/analysis/live-on-one-income'].some(x=>path.startsWith(x)))path+=`${path.includes('?')?'&':'?'}view_user_id=${view}`;const res=await fetch(base+path,{...init,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...init.headers}});if(!res.ok) throw new Error((await res.json().catch(()=>({detail:'Request failed'}))).detail);return res.status===204?null:res.json()}
export const money=(n:number|string)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(n));
