const base=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000/api/v1';
export async function api(path:string, init:RequestInit={}){const token=typeof window!=='undefined'?localStorage.getItem('pfos_token'):null;const view=typeof window!=='undefined'?localStorage.getItem('pfos_view_user_id'):null;if(view&&!path.includes('view_user_id=')&&!path.includes('view_scope=joint')&&['/dashboard','/transactions','/available-cash-planner','/planner-carryovers','/planner-adjustments','/planner-income-allocations','/planner-income-rules','/classification-rules','/classification-suggestions','/category-tracker','/reports'].some(x=>path.startsWith(x)))path+=`${path.includes('?')?'&':'?'}view_user_id=${view}`;const res=await fetch(base+path,{...init,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...init.headers}});if(!res.ok) throw new Error((await res.json().catch(()=>({detail:'Request failed'}))).detail);return res.status===204?null:res.json()}
export const money=(n:number|string)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(n));
