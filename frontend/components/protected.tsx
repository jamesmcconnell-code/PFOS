'use client';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';export function Protected({children}:{children:React.ReactNode}){const [ok,setOk]=useState(false),r=useRouter();useEffect(()=>{if(!localStorage.getItem('pfos_token'))r.replace('/login');else setOk(true)},[r]);return ok?<>{children}</>:<p className="p-8 text-slate-500">Loading your finances…</p>}
