'use client';
import Link from 'next/link';import {usePathname,useRouter} from 'next/navigation';import {LayoutDashboard,WalletCards,ReceiptText,Target,ChartNoAxesCombined,Settings,Upload,Plug} from 'lucide-react';
const links=[['/dashboard','Dashboard',LayoutDashboard],['/accounts','Accounts',WalletCards],['/transactions','Transactions',ReceiptText],['/import','CSV Import',Upload],['/connections','Connections',Plug],['/goals','Goals',Target],['/forecasting','Forecasting',ChartNoAxesCombined],['/settings','Settings',Settings]] as const;
export function Nav(){const path=usePathname(),router=useRouter();return <aside className="border-r border-slate-800 bg-navy text-slate-300 md:min-h-screen md:w-60"><div className="p-5 text-lg font-bold text-white">PF<span className="text-mint">OS</span><p className="mt-1 text-xs font-normal text-slate-400">Personal financial operating system</p></div><nav className="flex overflow-x-auto px-3 pb-3 md:block">{links.map(([href,label,Icon])=><Link key={href} href={href} className={`flex shrink-0 items-center gap-3 rounded-xl px-3 py-3 text-sm ${path===href?'bg-white/10 text-white':'hover:bg-white/5'}`}><Icon size={18}/>{label}</Link>)}</nav><button onClick={()=>{localStorage.removeItem('pfos_token');router.push('/login')}} className="mx-6 hidden text-xs text-slate-400 hover:text-white md:block">Log out</button></aside>}
